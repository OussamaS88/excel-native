import 'package:flutter/material.dart';

class FamilyDataPage extends StatelessWidget {
  const FamilyDataPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("family data");
  }
}
