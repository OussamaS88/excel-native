import 'package:flutter/material.dart';

class CampPage extends StatelessWidget {
  const CampPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [const Text("Camps")],
    );
  }
}
