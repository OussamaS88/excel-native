import '../family_detail.dart';
import 'package:flutter/material.dart';

class FamilyDetailPage extends StatelessWidget {
  const FamilyDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("d");
  }
}
